from textnode import TextNode, TextType


def main():
  # function should create a new TextNode object with dummy values
  text_node = TextNode("Sample text", TextType.TEXT)
  # Print the object to the console -- make sure it looks like what you expect
  print(text_node)
  

if __name__ == "__main__":
    main()
    
    